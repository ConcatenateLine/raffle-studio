name: Deploy release from source
on:
  repository_dispatch:
    types: [deploy_release]

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout target repo
        uses: actions/checkout@v4

      - name: Configure bot identity
        run: |
          git config user.name "ConcatenateLineMobile[bot]"
          git config user.email "mobile-bot@concatenateline.dev"

      - name: Create tag
        run: |
          git tag -a ${{ github.ref_name }} -m "Release ${{ github.ref_name }}"
          git push origin ${{ github.ref_name }}

      - name: Download artifact from source run
        env:
          OWNER: ${{ github.event.client_payload.source_owner }}
          REPO: ${{ github.event.client_payload.source_repo }}
          RUN_ID: ${{ github.event.client_payload.run_id }}
          GH_TOKEN: ${{ secrets.SOURCE_APP_READ_TOKEN }} # fine-grained PAT with Actions: read on source repo
          URL: ${{ github.event.client_payload.url }}
          TAG: ${{ github.event.client_payload.tag_name }}
          CHANGELOG: ${{ github.event.client_payload.changelog }}
        run: |
          set -e  # Exit immediately if any command fails

          # 1) Download release APK
          echo "🔍 Downloading release APK for tag ${TAG}..."
          
          # Add retry logic for download
          MAX_RETRIES=3
          RETRY_DELAY=5
          for i in $(seq 1 $MAX_RETRIES); do
            if RESPONSE=$(curl -Lo raffle-studio-${TAG}.apk $URL); then
              break
            fi
            if [ $i -eq $MAX_RETRIES ]; then
              echo "❌ Failed to download APK after $MAX_RETRIES attempts"
              exit 1
            fi
            echo "⚠️ Attempt $i failed, retrying in $RETRY_DELAY seconds..."
            sleep $RETRY_DELAY
          done

          # Verify download succeeded
          if [ ! -f "raffle-studio-${TAG}.apk" ]; then
            echo "❌ APK download failed"
            exit 1
          fi

          echo "✅ APK downloaded successfully: raffle-studio-${TAG}.apk"

          # 2) Create release notes file
          echo $CHANGELOG > CHANGELOG.md

          # Cleanup and finalize
          echo "✅ Success! APK downloaded and release notes created"

      - name: Build release
        run: echo ${{ github.sha }} > Release-${{ github.ref_name }}.txt
      
      - name: Test release
        run: cat Release-${{ github.ref_name }}.txt
      
      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          body: $CHANGELOG
          files: |
            Release-${{ github.ref_name }}.txt
            raffle-studio-${TAG}.apk
